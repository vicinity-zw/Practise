1.使用前需要安装chardet库，将chardet-2.3.0.tar.gz系统的目录中，指向安装命令。
   参考命令
   tar -xzvf chardet-2.3.0.tar.gz
   cd chardet-2.3.0
   python setup.py install
2. 手动运行脚本，在脚本所在目录输入命令python check_code.py即可。
3. 如果需要转换文件的编码，输入命令python convert.py即可执行。